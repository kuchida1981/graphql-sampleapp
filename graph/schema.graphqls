# GraphQL schema for Hello World sample

type Query {
  hello: String!
  messages: [Message!]!
  message(id: ID!): Message
}

type Message {
  id: ID!
  content: String!
  author: String!
  createdAt: String!
}
